# DSA-Lab-Programs
⚙️ Operations Used Push → Insert element into stack Pop → Remove top element Peek → View top element Display → Show all elements ⚠️ Conditions Overflow → When stack is full Underflow → When stack is empty ⏱ Time Complexity Push → O(1) Pop → O(1) Peek → O(1)
